# Dependencies (none required — zero-dependency project)
node_modules/

# Logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/

# Large demo recordings — kept in zip backups, not in git history
*.mp4
*.webm

# Secrets — never commit real API credentials
tools/reddit-credentials.json
